import { InfinteScrollDirective } from './infinte-scroll.directive';

describe('InfinteScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new InfinteScrollDirective();
    expect(directive).toBeTruthy();
  });
});
